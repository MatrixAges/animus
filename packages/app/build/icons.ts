import { writeFileSync } from 'fs'

// https://github.com/phosphor-icons/react submodule core/src/icons
// const icons = []

// const target = icons.reduce(
// 	(total, item) => {
// 		total[item.name] = item.tags
// 		return total
// 	},
// 	{} as Record<string, Array<string>>
// )

// writeFileSync(`${process.cwd()}/appdata/icons.ts`, `export default ${JSON.stringify(target)}`)
